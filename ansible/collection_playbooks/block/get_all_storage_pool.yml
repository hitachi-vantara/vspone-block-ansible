- 
  name: Get All Storage Pool details
  hosts: localhost
  collections:
    - hitachi.storage
  gather_facts: false
  vars:
    - storage_serial: 123456
  tasks:
    - hv_storagepool_facts:
        storage_serial: '{{ storage_serial  | default(None) }}'
      register: result
    - debug: var=result.storageSystem