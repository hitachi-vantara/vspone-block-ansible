-
  name: Remove WWNs from Host Group
  hosts: localhost
  collections:
    - hitachi.storage
  gather_facts: false
  pre_tasks:
    - include_vars: vars.ucpa.yml
  vars:
    - storage_serial: 611032
    - ucp_serial: UCP-CI-11111
    - host_group_name: test-ansible-hg-1
    - ports:
      - CL2-B
    - wwns:
      - 200000109B3C0FD3
  tasks:
    - hv_hg:
        state: present

        storage_system_info:
          serial: '{{ storage_serial }}'
          ucp_serial: '{{ ucp_serial }}'
          
        ucp_advisor_info:
          address: "{{ucpadvisor_address}}"
          username: "{{ucpadvisor_username}}"
          password: "{{ucpadvisor_password}}"

        spec:
          state: absent
          name: '{{ host_group_name }}'
          ports: '{{ ports  | default(None)  }}'
          wwns: '{{ wwns | default(None) }}'

      register: result
    - debug: var=result.hostGroups

