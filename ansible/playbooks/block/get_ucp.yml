-
  name: Get UcpSystem
  hosts: localhost
  collections:
    - hitachi.storage
  gather_facts: false
  vars:
    - serial_number: UCP-CI-12035
  tasks:
    - hv_ucp_facts:
        serial_number: '{{ serial_number | default(None) }}'
      register: result
    - debug: var=result

