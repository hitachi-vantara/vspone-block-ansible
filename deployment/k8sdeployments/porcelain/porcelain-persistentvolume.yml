apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-porcelain
  labels:
    type: local
spec:
  storageClassName: sc-porcelain
  capacity:
    storage: 30Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/home/porcelain/logs"
