apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-idm
  labels:
    type: local
spec:
  storageClassName: sc-idm
  capacity:
    storage: 30Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/home/idm/logs"
