
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.5
  creationTimestamp: null
  name: restorenetworkconfigurations.ucp.hitachivantara.com
spec:
  group: ucp.hitachivantara.com
  names:
    kind: RestoreNetworkConfiguration
    listKind: RestoreNetworkConfigurationList
    plural: restorenetworkconfigurations
    singular: restorenetworkconfiguration
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: RestoreNetworkConfiguration is the Schema for the restorenetworkconfigurations
        API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: RestoreNetworkConfigurationSpec defines the desired state of
            RestoreNetworkConfiguration
          properties:
            credentialsName:
              description: The name of the secret containing the SAD credentials (requires
                keys "username" and "password").
              type: string
            fileName:
              description: File name to save the switch configurations to a local
                flash drive or remote SCP/SFTP/TFTP server. When the proto parameter
                is not specified, the file will be saved to the local flash drive;
                the filePath, user, password, and host parameters will be ignored
                if specified. Saving to a local flash drive is not supported on the
                Brocade ICX switch; use the TFTP protocol instead.
              type: string
            filePath:
              description: The file path where the file name resides on remote host
                server
              type: string
            host:
              description: IP address or host name of the server
              type: string
            managementIP:
              description: EthernetSwitchName ethernet net switch  name used in creating
                custom resource
              type: string
            timestamp:
              type: string
            type:
              description: Network device type options are "ETHERNET" "FC"
              type: string
          type: object
        status:
          description: RestoreNetworkConfigurationStatus defines the observed state
            of RestoreNetworkConfiguration
          properties:
            result:
              description: 'INSERT ADDITIONAL STATUS FIELD - define observed state
                of cluster Important: Run "operator-sdk generate k8s" to regenerate
                code after modifying this file Add custom validation using kubebuilder
                tags: https://book-v1.book.kubebuilder.io/beyond_basics/generating_crd.html
                result'
              properties:
                isToBeRequeued:
                  type: boolean
                lastTimestamp:
                  type: string
                message:
                  type: string
                messageID:
                  type: string
                requeueWaitTime:
                  type: integer
                status:
                  type: string
              required:
              - lastTimestamp
              - message
              - messageID
              - status
              type: object
          type: object
      type: object
  version: v1
  versions:
  - name: v1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
