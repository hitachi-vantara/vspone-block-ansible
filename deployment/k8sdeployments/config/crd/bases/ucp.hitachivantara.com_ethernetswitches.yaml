
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.3.0
  creationTimestamp: null
  name: ethernetswitches.ucp.hitachivantara.com
spec:
  additionalPrinterColumns:
  - JSONPath: .spec.managementIP
    description: Management IP Address
    name: ManagementIP
    type: string
  - JSONPath: .status.details.model
    description: ethernet switch model
    name: Model
    type: string
  - JSONPath: .status.ucpSystem
    description: UcpSystem of ethernet switch
    name: UcpSystem
    type: string
  - JSONPath: .status.result.status
    description: Status
    name: Status
    type: string
  - JSONPath: .status.result.message
    description: Most recent message
    name: Message
    type: string
  group: ucp.hitachivantara.com
  names:
    kind: EthernetSwitch
    listKind: EthernetSwitchList
    plural: ethernetswitches
    shortNames:
    - ethsw
    singular: ethernetswitch
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: EthernetSwitch is the Schema for the ethernetswitches API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: EthernetSwitchSpec defines the desired state of EthernetSwitch
          properties:
            credentialsName:
              description: The name of the secret containing the SAD credentials (requires
                keys "username" and "password").
              type: string
            managementIP:
              description: 'INSERT ADDITIONAL SPEC FIELDS - desired state of cluster
                Important: Run "operator-sdk generate k8s" to regenerate code after
                modifying this file Add custom validation using kubebuilder tags:
                https://book-v1.book.kubebuilder.io/beyond_basics/generating_crd.html
                Address holds the IP Address for accessing management IP'
              type: string
            rBridgeId:
              description: Only available in Brocade network devices set up in fabric
                cluster mode. The ID must be a positive number and at least equal
                to 1 or greater
              type: string
            restoreConfigFile:
              type: string
            saveConfigFile:
              type: string
            syslogServerIps:
              description: SystemLogRemoteIP this is used if system log configuration
                need to be set to remote sys log server ip address syslogServerIps
                are used to set syslog configuration on ethernetswitch, omit this
                field will skip the operation
              items:
                type: string
              type: array
            timestamp:
              type: string
          required:
          - credentialsName
          - managementIP
          type: object
        status:
          description: EthernetSwitchStatus defines the observed state of EthernetSwitch
          properties:
            ID:
              description: unique ID this ethernet switch
              type: string
            details:
              description: Ethernet switch detail
              properties:
                100GBPorts:
                  description: Number of 100 GB ports
                  type: integer
                10GBPorts:
                  description: Number of 10 GB ports
                  type: integer
                25GBPorts:
                  description: Number of 25 GB ports
                  type: integer
                40GBPorts:
                  description: Number of 40 GB ports
                  type: integer
                currentTime:
                  description: Ethernet switch current time in format HH:MM::SS
                  type: string
                ethernetSwitchId:
                  description: Ethernet switch ID
                  type: string
                firmware:
                  description: Ethernet switch firmware version
                  type: string
                grid:
                  description: Global Resource ID
                  type: string
                hwRev:
                  description: Ethernet switch hardware version
                  type: string
                isJumboFrameCapable:
                  description: Ethernet switch jumbo frame capable flag
                  type: boolean
                macAddresses:
                  description: Ethernet switch (system burned-in) MAC address(es)
                  items:
                    type: string
                  type: array
                mgmtIP:
                  description: Ethernet switch management IP address
                  type: string
                mgmtPortStatus:
                  description: 'Ethernet switch management port status. Currently
                    supported values: ■ CONNECTED ■ UP ■ DOWN'
                  type: string
                mgmtPortStatusDescription:
                  description: Ethernet switch management port status description
                    if the port status is not UP nor CONNECTED.
                  type: string
                model:
                  description: Ethernet switch model
                  type: string
                name:
                  description: Ethernet switch name
                  type: string
                os:
                  description: Ethernet switch operating system information, if available
                  type: string
                portNum:
                  description: Ethernet switch part number
                  type: string
                serialNum:
                  description: Ethernet switch serial number
                  type: string
                status:
                  description: 'Ethernet switch status. Currently supported values:
                    ■ UP ■ DOWN ■ UNKNOWN'
                  type: string
                timezone:
                  description: Ethernet switch current time zone.  The format of the
                    time zone is determined by the vendor of the switch.
                  type: string
                uptime:
                  description: Ethernet switch uptime in format <DAY> days, <HOURS>
                    hours, <MINUTES> minutes, <SECONDS> seconds
                  type: string
              type: object
            latestRestoreConfigFile:
              description: LatestSaveConfigFile is used for infrastructure-operator
                to ensure certain operations run once
              type: string
            latestSaveConfigFile:
              description: LatestSaveConfigFile is used for infrastructure-operator
                to ensure certain operations run once
              type: string
            managementIP:
              description: Management IP
              type: string
            result:
              description: result
              properties:
                isToBeRequeued:
                  type: boolean
                lastTimestamp:
                  type: string
                message:
                  type: string
                messageID:
                  type: string
                requeueWaitTime:
                  type: integer
                status:
                  type: string
              required:
              - lastTimestamp
              - message
              - messageID
              - status
              type: object
            ucpSystem:
              description: list of Ethernet ports Ports []EthernetPort `json:"ports,omitempty"`
                list of Ethernet MAC MACAddresses []EthernetMAC `json:"macAddresses,omitempty"`
                list of Ethernet Switch Configurations Configurations []NetworkSwitchConfiguration
                `json:"configuration,omitempty"` list of Ethernet swith vlans VLans
                []VLanDevice `json:"vlans,omitempty"` list of syslog configs SysLogConfigs
                []SyslogConfig `json:"sysLogConfigs,omitempty"` UCP system serial
                this ethernet switch is part of if not nil
              type: string
          type: object
      type: object
  version: v1
  versions:
  - name: v1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
