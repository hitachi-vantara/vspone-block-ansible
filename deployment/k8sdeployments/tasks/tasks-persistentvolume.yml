apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-tasks
  labels:
    type: local
spec:
  storageClassName: sc-tasks
  capacity:
    storage: 30Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/home/tasks/logs"
