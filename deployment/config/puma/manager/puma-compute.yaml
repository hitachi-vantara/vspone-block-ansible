apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    puma-compute-service: sdi-gateway-compute
  name: sdi-gateway-compute
spec:
  replicas: 1
  selector:
    matchLabels:
      puma-compute-service: sdi-gateway-compute
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        puma-compute-service: sdi-gateway-compute
    spec:
      containers:
      - env:
        - name: LD_LIBRARY_PATH
          value: /opt/hitachi/puma
        - name: TZ
          value: America/Los_Angeles
        - name: _REDIS_HOST
          value: redis
        - name: _REST_SERVER_CONSOLE_LOG
          value: "true"
        - name: _REST_SERVER_LIBRARY_LOG_LEVEL
          value: verbose
        - name: _REST_SERVER_LOG_LEVEL
          value: verbose
        - name: _REST_SERVER_CERTS_DIR
          value: /var/certs
        - name: _REST_SERVER_CERT
          value: tls.crt
        - name: _REST_SERVER_KEY
          value: tls.key
        image: cpumacompute:8.6.0
        name: sdi-gateway-compute
        ports:
        - containerPort: 8444
        resources: {}
        volumeMounts:
        - mountPath: /var/log
          name: sdi-gateway-compute-hostpath0
      restartPolicy: Always
      volumes:
      - hostPath:
          path: /var/docker/log
        name: sdi-gateway-compute-hostpath0
