
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.3.0
  creationTimestamp: null
  name: backupnetworkswitchconfigurations.ucp.hitachivantara.com
spec:
  additionalPrinterColumns:
  - JSONPath: .spec.schedule
    description: Defined schedule as CRON string
    name: Schedule
    type: string
  - JSONPath: .spec.ucpSystem
    description: Specified UCP system
    name: UCP System
    type: string
  - JSONPath: .status.result.status
    description: Status
    name: Status
    type: string
  - JSONPath: .status.result.message
    description: Most recent message
    name: Message
    type: string
  group: ucp.hitachivantara.com
  names:
    kind: BackupNetworkSwitchConfiguration
    listKind: BackupNetworkSwitchConfigurationList
    plural: backupnetworkswitchconfigurations
    singular: backupnetworkswitchconfiguration
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: BackupNetworkSwitchConfiguration is the Schema for the backupnetworkswitchconfigurations
        API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: BackupNetworkSwitchConfigurationSpec defines the desired state
            of BackupNetworkSwitchConfiguration
          properties:
            networkDeviceIPAddress:
              description: The name of the network device Optional when UCPSystem
                is provided
              type: string
            remoteBackup:
              description: If remote backup is set to true backup will be taken in
                remote scp server. Default is true
              type: boolean
            schedule:
              description: 'Schedule must be in CRON format If not mentioned, the
                backup will be initiated instantly Example CRON format for running
                every 30 minutes: "0 0/30 * 1/1 * ? *" Example CRON format for running
                every Saturday at midnight: "0 0 0 ? * SAT"'
              type: string
            timestamp:
              description: TimeStamp can be used to force update on backup, currently
                not used in any logic, no required format, as long as different from
                previous
              type: string
            ucpSystem:
              description: The name of the UCP system Required for backup config of
                all switches in UCPSystem
              minLength: 5
              pattern: ^(UCP-CI|UCP-HC|UCP-RS|UCP-2000|UCP-4000|Logical-UCP)-\d{5}$
              type: string
          type: object
        status:
          description: BackupNetworkSwitchConfigurationStatus defines the observed
            state of BackupNetworkSwitchConfiguration
          properties:
            observedGeneration:
              format: int64
              type: integer
            result:
              description: result
              properties:
                isToBeRequeued:
                  type: boolean
                lastTimestamp:
                  type: string
                message:
                  type: string
                messageID:
                  type: string
                requeueWaitTime:
                  type: integer
                status:
                  type: string
              required:
              - lastTimestamp
              - message
              - messageID
              - status
              type: object
          type: object
      type: object
  version: v1
  versions:
  - name: v1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
