apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: local-fc-pvc
spec:
  accessModes:
   - ReadWriteOnce
  #storageClassName: local-storage
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi
