# permissions for end users to view resetmultipleserversbmcpasswords.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: resetmultipleserversbmcpassword-viewer-role
rules:
- apiGroups:
  - ucp.hitachivantara.com
  resources:
  - resetmultipleserversbmcpasswords
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - ucp.hitachivantara.com
  resources:
  - resetmultipleserversbmcpasswords/status
  verbs:
  - get
