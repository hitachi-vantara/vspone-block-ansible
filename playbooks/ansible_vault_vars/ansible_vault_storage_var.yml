#User will use Ansible Vault to encrypt this file.
#Ansible will decrypt this file when it executes the playbook.


storage_serial: 440123
storage_address: 10.1.1.1
storage_ansible_vault_user: user
storage_ansible_vault_secret: secret
