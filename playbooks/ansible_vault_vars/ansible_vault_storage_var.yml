#User will use Ansible Vault to encrypt this file.
#Ansible will decrypt this file when it executes the playbook.

storage_serial: YOUR_STORAGE_SERIAL_NUMBER
storage_address: YOUR_STORAGE_IP_ADDRESS
ansible_vault_storage_username: YOUR_STORAGE_USERNAME
ansible_vault_storage_secret: YOUR_STORAGE_PASSWORD
